@using PersianProcess.NCS.Model
@using System.Data.Entity
@model DbSet<EvenNumber>

<ul class="list-inline">
    <li class="">Evens</li>
    @foreach (var item in Model) {
      <li class="">@item.Number</li>
    }
</ul>